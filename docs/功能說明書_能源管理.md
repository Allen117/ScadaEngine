# 功能說明書：能源管理（電表/迴路設定 + 用電報表）

## 1. 功能概述

能源管理模組讓使用者建立**多層樹狀的電表/迴路階層**，並依時/日/月/年四種單位查詢用電量。整體採 **on-demand 即時計算**，不預存加總值，HistoryData 為唯一資料來源。

### 兩個子頁面

| 路由 | 功能 |
|------|------|
| `/EnergyMeter` | 電表/迴路階層 CRUD（樹狀新增、編輯、刪除） |
| `/EnergyReport` | 選一個迴路 + 單位查詢用電，含長條圖 + 表格 + Excel 匯出 |

### 核心概念

- **迴路節點**：階層的最小單元
  - **實體電表**：綁定一個 SID（`ModbusPoints` 或 `CalculatedPoints` 提供累積 kWh），可設 `MaxKwh`
  - **虛擬迴路**：不綁 SID，用電量 = 所有後代葉子的 delta **帶 sign 加權**加總
- **貢獻方向（Sign）**：每個節點對其父節點的方向（+1 / −1，預設 +1）。葉子的有效權重 = 從查詢根到葉路徑上所有 sign 的乘積（不含查詢根本身）。詳見 §5.5
- **單位與期間繫結**（防誤操作 + 控制效能）：

  | 單位 | 期間選擇器 | bucket 數 |
  |------|------------|-----------|
  | 時 | 起訖時（可跨日/月/年） | 起時 ~ 訖時，每小時 1 bucket |
  | 日 | 起訖日（可跨年） | 起日 ~ 訖日，每日 1 bucket |
  | 月 | 起訖月（可跨年） | N 個月 |
  | 年 | 起訖年 | N 年 |

- **kWh 溢位/重置處理**：
  - V_end ≥ V_start：delta = V_end − V_start
  - V_end < V_start 且有 MaxKwh：delta = (MaxKwh − V_start) + V_end
  - V_end < V_start 且無 MaxKwh：delta = 0，記 log warning，回傳結果旗標 `isHasWarning=true`

---

## 2. 路由

### 電表/迴路設定
| 方法 | 路由 | 說明 | 認證 |
|------|------|------|------|
| GET | `/EnergyMeter` | 設定頁（樹狀 UI） | 需登入 |
| GET | `/EnergyMeter/api/tree` | 取得所有節點（平坦清單） | 需登入 |
| GET | `/EnergyMeter/api/sids` | SID 下拉清單（Modbus + Calculated + DB；含 `source` / `coordName` 通訊設備名 / `deviceName` 子單元名，供三層連動下拉） | 需登入 |
| GET | `/EnergyMeter/api/points` | **全部**點位清單（不限 kWh，欄位同 api/sids）— 電表資訊自動比對與兩步驟選點器用 | 需登入 |
| GET | `/EMS/api/main-meter-info` | 主要電表資訊卡資料：`{ hasMainMeter, mode, name, voltage, current, power, powerFactor }`。`mode='realtime-by-sid'`（實體主表：per-role `{sid,pointName,unit}`，前端走 `POST /api/realtime/by-sids` 輪詢）或 `mode='aggregated'`（虛擬主表：per-role `{unit}`，前端走 `GET /EMS/api/main-meter-values` 輪詢聚合值）；無主要電表回 `{ hasMainMeter:false }` | 需登入 |
| GET | `/EMS/api/main-meter-values` | 虛擬主要電表 V/I/P/PF 聚合值 `{ voltage, current, power, powerFactor }`（皆 `number\|null`）；由 `MainMeterAggregationService.ComputeAsync` 現算，實體主表不走此 API | 需登入 |
| POST | `/EnergyMeter/api/tree` | 新增節點 | 需登入 |
| PUT | `/EnergyMeter/api/tree/{id}` | 更新節點（不動 ParentId/SortOrder） | 需登入 |
| DELETE | `/EnergyMeter/api/tree/{id}` | 刪除節點，含子節點時回 409，需 `?force=true` | 需登入 |
| PUT | `/EnergyMeter/api/tree/sort` | 批次更新排序與 ParentId | 需登入 |

### 用電報表
| 方法 | 路由 | 說明 | 認證 |
|------|------|------|------|
| GET | `/EnergyReport` | 報表頁 | 需登入 |
| GET | `/EnergyReport/api/circuits` | 取得迴路樹（給下拉選單） | 需登入 |
| POST | `/EnergyReport/api/query` | 查詢報表（回 `EnergyReportResult` JSON） | 需登入 |
| POST | `/EnergyReport/api/export` | 匯出 .xlsx（回檔案） | 需登入 |

---

## 3. 檔案結構

```
ScadaEngine.Common/
└── Data/Models/
    ├── EnergyCircuitModel.cs          ← 對應 EnergyCircuit DB 表
    └── EnergyReportResult.cs          ← 報表計算結果（含 buckets 陣列）

ScadaEngine.Web/
├── Features/
│   ├── EnergyMeter/
│   │   ├── Controllers/EnergyMeterController.cs
│   │   ├── Models/EnergyCircuitNodeViewModel.cs
│   │   ├── Models/EnergyCircuitRequestDtos.cs
│   │   └── Views/Index.cshtml
│   └── EnergyReport/
│       ├── Controllers/EnergyReportController.cs
│       ├── Models/EnergyReportRequestDto.cs
│       ├── Models/EnergyReportViewModel.cs
│       └── Views/Index.cshtml
├── Services/
│   ├── EnergyCircuitService.cs        ← 樹狀 CRUD + 遞迴展開葉子
│   ├── EnergyReportService.cs         ← 邊界值查詢 + 溢位 delta + 加總
│   └── EnergyReportExcelExporter.cs   ← ClosedXML 封裝
└── wwwroot/
    ├── css/energymeter.css            ← 含電表資訊 Modal + 兩步驟選點器樣式
    ├── css/energyreport.css
    ├── js/role-aliases.js             ← 電表角色別名表共用模組（window._roleAliases，Designer 列範本共用）
    ├── js/energymeter.js              ← 含電表資訊設定（自動比對 + 選點器）
    └── js/energyreport.js

ScadaEngine.Engine/DatabaseSchema/DatabaseSchema.json   ← +EnergyCircuit 表
```

---

## 4. 資料表

### EnergyCircuit（自參照樹）

| 欄位 | 型別 | 說明 |
|------|------|------|
| Id | INT IDENTITY PK | |
| Name | NVARCHAR(100) | 顯示名稱 |
| ParentId | INT NULL | 父節點 Id；NULL = 根 |
| SortOrder | INT NOT NULL | 同層排序 |
| SID | NVARCHAR(100) NULL | 綁 ModbusPoints/CalculatedPoints；NULL = 虛擬迴路 |
| MaxKwh | FLOAT NULL | 累積最大值，溢位處理用 |
| Sign | INT NOT NULL DEFAULT 1 | 對父節點貢獻方向：+1 加入 / −1 扣減；根節點固定 +1 |
| IsMainMeter | BIT NOT NULL DEFAULT 0 | 主要電表標記 — **全系統唯一**（勾新的自動取消舊的，應用層保證）；**實體 / 虛擬皆可**（虛擬主表由 EMS 頁自動聚合子孫葉子的 V/I/P/PF）。既有 DB 由 `MigrateColumnsAsync` 於 Engine 啟動時自動 `ALTER TABLE ADD` |
| VoltageSID | NVARCHAR(100) NULL | 電表資訊 — 電壓點位 SID；**僅實體電表可存**（無論是否為主要電表；虛擬迴路伺服器端強制 NULL）。既有 DB 由欄位同步機制自動補上 |
| CurrentSID | NVARCHAR(100) NULL | 電表資訊 — 電流點位 SID（規則同上） |
| PowerSID | NVARCHAR(100) NULL | 電表資訊 — 功率點位 SID（規則同上） |
| PowerFactorSID | NVARCHAR(100) NULL | 電表資訊 — 功因點位 SID（規則同上） |
| Description | NVARCHAR(500) NULL | 備註 |
| CreatedAt | DATETIME NOT NULL | |
| UpdatedAt | DATETIME NULL | |

> 自參照 FK 不加實體 constraint，由應用程式保證一致性（同 `LogicFlowTree`、`ScadaDesignPage`）。

---

## 5. 報表算法（關鍵流程）

### Step 1：bucket 邊界產生
產出 **N 個 bucket 的 [起, 訖) 邊界對**（`BuildBucketRangesAsync`）：

| 單位 | bucket 邊界 |
|------|-------------|
| hour | 起時 ~ 訖時隔小時，每小時一格（連續切界，相鄰 bucket 共用邊界點） |
| day | 起日 00:00 ~ 訖日隔日 00:00，每天一格（連續切界） |
| month | **月結期別切界** — YYYY-MM 為期別編號，每期一對 [期別起始 00:00, 結束日隔日 00:00)，期界由 `BillingPeriodService` 解析（見 §12 月結週期設定）；期別間可能空窗/重疊，不共用邊界點 |
| year | 起訖年每年 1/1，每年一格（維持自然年，不隨期別偏移） |

月 bucket 標籤：期別 = 自然月時維持 `yyyy-MM`；非自然月顯示完整期間 `yyyy-MM-dd~MM-dd`（跨年右端帶年份），Excel 匯出同步。

### Step 2：取邊界值（OUTER APPLY 一條 SQL）
對每個葉子，組 `VALUES` 列表加 `OUTER APPLY` 取每個邊界 ≤ 該時刻的最近一筆：

```sql
SELECT b.idx, ba.Value
FROM (VALUES (@i0,@t0), (@i1,@t1), ...) AS b(idx, BoundaryTime)
OUTER APPLY (
    SELECT TOP 1 Value FROM HistoryData WITH (NOLOCK)
    WHERE  SID = @sid AND Timestamp <= b.BoundaryTime AND Quality = 1
    ORDER BY Timestamp DESC
) ba
ORDER BY b.idx;
```

每個 APPLY 命中 `[SID, Timestamp]` clustered seek，O(log N)。

### Step 3：相鄰邊界相減 + 溢位處理 + 加總
- 對每個葉子算出 N 個 delta，套溢位規則（**raw delta 永遠 ≥ 0**，溢位邏輯不受 sign 影響）
- 各葉子的 delta 乘上 effective sign 後同位置相加 → 該迴路該 bucket 的 kWh（**可為負值**）
- 全 bucket 加總 → 總 kWh

### Step 5：貢獻方向（Sign）— 支援 A+B+C−D

每個節點多一欄 `Sign` (+1 / −1，預設 +1)，表示該節點對父節點的貢獻方向。展開葉子時，從查詢根遞迴往下，每層 effective sign = 父 effective sign × 自己 sign（**查詢根本身不計**）。

A+B+C−D 的建模：

```
總計 (Sign=+1, 虛擬, 根)
├── A (Sign=+1, 葉子)
├── B (Sign=+1, 葉子)
├── C (Sign=+1, 葉子)
└── D (Sign=−1, 葉子)
```

報表查詢「總計」時：A、B、C 的 effective sign = +1；D = −1。bucket kWh = A_delta + B_delta + C_delta − D_delta。

**整支子樹反向**：把 D 換成「D 群（含 D1、D2、D3）」，只要 D 群 Sign=−1，其下所有葉子自動反向（−1 × +1 = −1）。

**規則**：

- 根節點（`ParentId IS NULL`）強制 Sign=+1（service / Controller / UI 三層保護），UI 隱藏該選項
- DB 不加 CHECK constraint，由 service / Controller 層驗證 ±1（與專案「不加 FK constraint」慣例一致）
- 溢位規則套在葉子原始 delta（物理意義：實體電表累計值不受 sign 影響），sign 只在最後合併時套用
- 報表 bucket / 子迴路欄位 / Excel 匯出皆會出現負值；Chart.js 不指定 `beginAtZero` 以正確顯示負 bar

**責任歸屬提醒（重要）**：計算點公式內 (`A - B`) 與迴路 sign (Sign=−1) **兩條路都能做減法**。系統不偵測重複扣減；若同一份計算點同時被公式與迴路 sign 扣減，使用者自負其責。建議擇一即可。

### Step 4：Excel 匯出展開直接子（僅匯出路徑）
`/api/export` 走 `GetReportWithChildrenAsync`：先用 Step 1~3 算父，再對父的每個**直接子節點**獨立呼叫一次 `ComputeBucketSumsForCircuit`（內部走相同 Step 2~3）。子若為虛擬節點，`GetLeavesUnderAsync` 仍會遞迴展開葉子，所以子欄就是「該子整棵子樹的合計」。`/api/query`（前端 chart/table）維持原 `GetReportAsync`，不含 children 欄位以節省 payload。實體葉子（自身綁 SID）查詢則略過子層展開，匯出格式維持兩欄。

---

## 6. 權限

兩個路由都註冊在 `PermissionService.ConfigurablePages`：
- `/EnergyMeter` — 電表/迴路設定
- `/EnergyReport` — 用電報表

帳號管理頁會自動列出這兩項供勾選。`_Layout.cshtml` 的「能源管理」下拉透過 `canAccess()` 條件渲染，未授權的帳號看不到也擋 URL 直接存取。

---

## 7. UI 操作步驟

### 電表/迴路設定
1. 點右上「+ 新增點表/迴路」建立第一層（例如「水系統」）
2. 在節點上 hover 出現操作鈕，點 + 新增子節點（例如「冰機群 → 冰機 1」）
3. 子節點選擇「實體電表」時，以三層連動下拉挑選點位，再填入 MaxKwh：
   - **通訊設備**：依來源以 optgroup 分組（Modbus 通訊設備 / 計算點位 / DB 來源），僅列出旗下有 kWh 點位的設備
   - **子單元**：僅多 ModbusID 協調器出現此層（列出有 kWh 點位的子設備）；單 ID 協調器、計算點位、DB 來源自動隱藏此層
   - **度數對應點位**：僅列出所選設備（或子單元）下單位為 kWh 的點位
4. **貢獻方向**：新增/編輯時切換「正向 / 反向」決定該節點是被加入還是從父扣減（根節點看不到此選項）
5. **主要電表**：**實體 / 虛擬迴路皆可勾選「主要電表」**，全系統僅能有一顆 — 勾選新的時原本的主要電表自動取消；樹狀清單以金色 ★ 標示、詳情區顯示 badge
   - 實體主表：詳情區顯示 4 個 V/I/P/PF 綁定點位（本節「實體電表 — 電表資訊設定」流程），EMS 頁走 `by-sids` 拉即時值
   - 虛擬主表：詳情區顯示「自動聚合」提示 + 參與聚合的子孫葉子清單（每葉子有哪些 V/I/P/PF 綁定以 badge 標示），EMS 頁走聚合 API
6. 反向節點在樹狀清單會顯示紅色 `−` badge 提示
7. 編輯/刪除：用 hover 顯示的 ✎ / 🗑 鈕；刪除有子節點時跳警告
8. 詳情區可看完整資訊（含貢獻方向、主要電表；主要電表另列電壓/電流/功率/功因四個綁定點位）與快速動作

### 實體電表 — 電表資訊設定（電壓/電流/功率/功因）

1. Modal 選擇「實體電表」後即出現「**電表資訊設定**」按鈕（**不再要求勾主要電表**；虛擬主表由子孫自動聚合，實體電表則是聚合來源，兩種情境都需要這 4 個 SIDs 存在），按鈕下方顯示已綁定數量摘要（已綁定 n/4）
2. 點按鈕開啟電表資訊 Modal（循序切換，不疊窗）：四列 電壓/電流/功率/功因，各列顯示已綁點位（設備 - 子單元 - 點名 + 單位）或「未設定」
3. **自動比對建議**：開啟時以度數點位名稱為基準，在**相同來源＋通訊設備＋子單元**內比對候選（排除度數點位本身），有候選時頂部顯示建議卡，可「全部帶入」或逐列「帶入」；找不到候選則不顯示建議卡
   - 比對採用與 Designer 列範本共用的 `ROLE_ALIASES` 別名表（`wwwroot/js/role-aliases.js`，掛 `window._roleAliases`），取 V / A / KW / PF 四組
   - 評分規則：點名尾段命中別名 +3、點名整體命中 +3、點名包含別名（≥2 字元）+2、單位命中 +2（電壓 V、電流 A、功率 kW/W/MW、功因 PF/%/空白）、含度數點名 stem +1；分數 ≤ 0 視為無候選，同分取清單序最小者
4. 每列「選擇」開啟**兩步驟選點器**：左側設備清單（多子單元可展開）→ 右側點位清單＋關鍵字搜尋（點名 / SID），已綁定點位自動高亮定位；「清除綁定」可清空該列
5. 四個點位皆可留空（不做必填檢核）；單位一律取綁定點位本身的 Unit，不寫死
6. 4 SID 暫存於前端，隨「儲存」一次寫入 DB；**虛擬迴路儲存時伺服器端強制清空 4 欄**（虛擬節點無物理電表意義），實體電表則無此限制
7. **/EMS 顯示**：需量卡片列上方「主要電表資訊」卡片依主要電表類型分派資料來源：
   - **實體主表** → `mode='realtime-by-sid'`，4 格顯示主表自身 V/I/P/PF 綁定點位的即時值，走 `POST /api/realtime/by-sids` 每 5 秒輪詢
   - **虛擬主表** → `mode='aggregated'`，4 格顯示「自動聚合」計算結果（詳見下節），走 `GET /EMS/api/main-meter-values` 每 5 秒輪詢
   - 未綁定 / 無聚合來源欄位顯示「未設定」或「--」，無主要電表整卡隱藏

### 虛擬主要電表 — 電表資訊自動聚合

虛擬迴路作為主要電表時，本身無綁 V/I/P/PF SIDs（伺服器端強制 NULL），改由 `MainMeterAggregationService.ComputeAsync` 從**子孫葉子的電表資訊** on-demand 聚合：

**演算法**（`ScadaEngine.Web/Services/MainMeterAggregationService.cs`）：

| 角色 | 聚合方式 | 說明 |
|------|----------|------|
| 電壓 V | 取第一顆綁 VoltageSID 且 Q=GOOD 的葉子（依 SortOrder → nId 排序） | 平行匯流上各分路電壓幾乎相同，取任一即可；不做平均以免破壞物理語意 |
| 電流 I | `Σ (I_i × sign_i)`，僅葉子有 CurrentSID 且 Q=GOOD 才入計 | 反向葉子（Sign=−1）依物理意義扣減 |
| 功率 P | `Σ (P_i × sign_i)`，僅葉子有 PowerSID 且 Q=GOOD 才入計 | 同上 |
| 功因 PF | `ΣP_pf / √(ΣP_pf² + ΣQ²)`，其中 `Q_i = P_i × tan(arccos(PF_i))`；僅葉子**同時有 PowerSID 與 PowerFactorSID 且皆 GOOD** 才入計 | 功因不可直接平均（±12% 誤差），必須走 P/Q 反推；sign 同時套在 P、Q 上 |

**邊界**：

- 任一角色無有效葉子 / 全部 Q=BAD/STALE → 該格回 null，前端顯示「--」
- PF 樣本 `≤0` 或 `>1` 略過（PF=0 代表沒功率、也沒角度資訊）
- ΣP_pf² + ΣQ² < 1e-9 → PF 回 null（避免除以 0）
- 單位取「參與聚合的第一顆葉子」對應角色 SID 的 unit（無綁定 → unit=""）

**注意事項**：

- 演算法不判斷功因方向（leading vs lagging）— 沿用使用者原始資料
- 電流/功率相加**假設所有子迴路在同一 bus 同一相**，這是使用者建樹時應負的物理責任（同 §5.5 sign 語意）
- 聚合值不落 DB、不預聚合 — 每次 API 呼叫直接讀即時 cache 並算，改子孫綁定即時生效不需回填

### 用電報表
1. 選一個迴路（樹狀縮排顯示）
2. 選單位，期間選擇器自動切換
3. 按「查詢」→ 看圖表與表格
4. **圖表「顯示明細 / 顯示合計」切換按鈕**（僅虛擬父迴路且直接子 ≥ 2 個時出現於圖表 card-header 右側）
   - 預設「合計」：單色長條，與既有行為一致
   - 切到「明細」：堆疊長條，每個直接子迴路一個 dataset、顏色不同，hover tooltip 列出各子迴路的 kWh + 該 bucket 合計
   - legend 列出所有子迴路名稱對應顏色
   - 子迴路若本身仍是虛擬節點，明細視圖只展開到該子層（其內部仍為合計），與 Excel 匯出語意一致
   - 每次重查強制回到「合計」預設，不保留上次切換狀態
   - 實體電表葉子（自身綁 SID）或無直接子的虛擬節點不顯示此按鈕
5. 按「匯出 Excel」下載 .xlsx（含查詢條件 + 數據表 + 合計）
   - 若選的是**虛擬父迴路**，Excel 額外多出每個**直接子迴路**一欄，逐 bucket 列出該分項 kWh，便於對照各分項佔比
   - 若選的是**實體電表葉子**（自身綁 SID），維持原本「時段 | 用電量」兩欄格式
   - 同列：父迴路欄 ≈ 各子迴路欄總和（受 Math.Round(3) 影響，誤差 < 0.001 kWh）
   - 子迴路若本身仍是虛擬節點，其值為自己整棵子樹的遞迴加總，不再展開

---

## 8. 套件相依

- **ClosedXML 0.102.2** — Excel 匯出（純 .NET，無 Office 依賴；EPPlus 5+ 需商業授權，故避開）

---

## 9. 設計取捨

- **自參照單表 vs 雙表**：雙表（meter + circuit）需要 join，價值不明，採單表
- **on-demand vs 預聚合**：純 on-demand 在「統計儀表板需查 100 葉子 × 10 年」時會打 ~13,000 次 index seek，冷快取下延遲 5~10 秒 → 加入**葉子層 hourly 預聚合**（見 §11）；階層加總仍 on-demand 保留結構即時生效
- **單位綁固定期間選擇器**：避免使用者選出語意不明的範圍（例如「年單位 + 任意起訖日期」），同時控制 bucket 數量
- **kWh 倒退無 MaxKwh 回 0**：fail-soft，由 log + warning flag 通知使用者補設

---

## 10. 注意事項

- HistoryData 採樣頻率不均時，邊界值可能差到 ±採樣間隔；對 kWh 累積值影響微小（<<0.1%），未特別處理
- Excel 匯出檔名含時間戳：`EnergyReport_{迴路名}_{單位}_{yyyyMMddHHmmss}.xlsx`
- 排序與 ParentId 移動的 API 已預留，前端拖曳排序可未來再開

---

## 11. 葉子層 Hourly 預聚合機制（Engine）

### 11.1 為什麼需要

未來儀表板需要在單頁上同時查「100 個葉子 × 24 小時 × 雙年同期」，純 on-demand 對 HistoryData 跑 OUTER APPLY 在冷快取下會打 ~13,000 次 index seek，延遲 5~10 秒，且每次切時間期間都是冷算。第一性原理：把「葉子 × 小時」這個**最小不可分聚合原子**物化到新表，把查詢成本從 `O(buckets × leaves)` 的 OUTER APPLY 降到一條 `GROUP BY` 的 range scan。**Sign 權重、虛擬節點階層加總仍記憶體做** — 保住「迴路結構改變即時生效不需回填」的紅線。

### 11.2 資料表 `EnergyLeafHourly`

| 欄位 | 型別 | 說明 |
|------|------|------|
| SID | nvarchar(50) | PK，葉子 SID |
| HourStart | datetime | PK，小時起點（local time，與 HistoryData.Timestamp 同基準） |
| DeltaKwh | float | 該小時 kWh 增量（已套溢位、未套 sign） |
| Quality | tinyint | 1=Good, 0=掉線 transition |
| IsRolledOver | bit | 是否觸發 MaxKwh 溢位修正 |
| CreatedAt | datetime | UPSERT 時間 |

### 11.3 Sparse Storage 三段語意

`EnergyLeafAggregator.ComputeAsync(sid, hourStart, maxKwh, maxStalenessHours)`：

- **兩邊界都有值** → 寫入 `Q=1, Delta=計算值`
- **只缺一邊**（staleness 過期或 SID 剛上線首小時）→ 寫入 `Q=0, Delta=0`（給儀表板畫灰 bar 標示掉線 transition）
- **兩邊都缺**（長期斷線中段 / 從沒寫過的 SID）→ **不寫該列**

Web 查詢以 `GROUP BY HourStart` 後在記憶體用 dictionary lookup 補 0（24 個迴圈、零成本），不需 LEFT JOIN。

### 11.4 邊界值 Staleness Window

`appsettings.json:EnergyAggregation.MaxStalenessHours`（預設 2）。`EnergyReportService` 與 `EnergyLeafAggregator` 兩條路徑**都套同一份設定**，確保「當前未完成那小時 on-demand 算」與「上個小時讀 EnergyLeafHourly」的數字連續。

**修正的 bug**：原 `EnergyReportService` 取「最近一筆」無時間上限，電表斷線數天/月後復原會產生「斷線期間每小時 = 0、復原首小時 = 整段累積巨柱」的錯誤。加 staleness window 後：斷線期間 sparse（空白）、恢復首小時 Q=0 灰 bar、恢復第 2 小時起正常 Q=1。

### 11.5 觸發機制

| 觸發 | 何時跑 | 範圍 |
|------|--------|------|
| Hourly Timer | 每小時 XX:02:00 | 上個小時（XX:00 ~ XX+1:00） |
| 啟動 Catch-up | Engine 重啟時 | 過去 `CatchUpHours` 小時（預設 24） |
| MQTT Backfill | 收到 `SCADA/Sys/EnergyLeafHourly/Backfill` | payload 指定 sid + 期間 |

XX:02 而非 XX:00 是給 DB 來源 polling buffer（避免最末幾筆 HistoryData 還沒寫入）。

### 11.6 Backfill MQTT 訊號

```
Topic:   SCADA/Sys/EnergyLeafHourly/Backfill   QoS=1, Retain=false
Payload: { "sid": null | "DB1-S5", "from": "yyyy-MM-dd", "to": "yyyy-MM-dd" }
```

- `sid=null` → 全葉子（典型：首次全量補算）
- `sid="xxx"` → 只算該 SID（典型：MaxKwh 改動後重算歷史）
- 內部按月分批 + 每批 sleep `BackfillBatchSleepMs`（預設 500ms）避免阻塞線上查詢
- UPSERT (MERGE) on `(SID, HourStart)`，與 hourly Timer 重疊也安全

### 11.7 設定區塊（`appsettings.json`）

```json
"EnergyAggregation": {
  "MaxStalenessHours": 2,        // 兩邊都用（Engine + Web）
  "CatchUpHours": 24,            // Engine 啟動補算範圍
  "BackfillBatchSleepMs": 500    // Backfill 每月之間 sleep
}
```

Engine 與 Web 都要設 `MaxStalenessHours` — Engine 用於聚合，Web 用於 on-demand 邊界值查詢。Web 端不需要 `CatchUpHours` / `BackfillBatchSleepMs`。

### 11.8 為什麼 sign / 階層加總不一起預聚合

迴路結構在生產環境會被動 — 新增子表、合併、改變 sign 都很常見。如果連 sign 都預聚合，每次調整都要回填整段歷史；保留「葉子原子」+ 階層 on-demand 計算的設計，**改 sign / 新增虛擬節點 / 改變父子關係即時生效**，不需任何回填動作。葉子層的 MaxKwh 改動是少數需要回填的情境 — 由前述 Backfill MQTT 訊號解決。

---

## 12. 月結週期設定（期別）

路由 `/BillingPeriodSetting`（EMS 體系，能管設定下拉選單；`ConfigurablePages` 可控管）。
實務上電費單結算週期不是 1 號起算且**每期抄表日會浮動**（例：6 月期 = 6/16～7/17、7 月期 = 7/18～8/16），
此頁讓**每個月（期別）各自設定起始/結束日期**，全系統月粒度報表統一以期別切 bucket。

### 12.1 資料表 `BillingPeriods`

| 欄位 | 型別 | 說明 |
|------|------|------|
| PeriodYear + PeriodMonth | int, 複合 PK | 期別編號（= 起始所在月份，台電慣例） |
| StartDate | datetime | 起始日 00:00（含） |
| EndDate | datetime | 結束日 00:00（**inclusive** — 該日整天算入本期；計算時 exclusive 邊界 = EndDate + 1 天） |
| UpdatedAt | datetime | 最後更新時間 |

**只存被使用者自訂過的期別**；沒有 row 的期別由 `BillingPeriodService` 推導，推導值不落 DB（避免污染未來月份）。

### 12.2 期別解析規則（`BillingPeriodService`）

1. 有自訂 row → 直接採用
2. 無 row → 起始 = **前一期結束 +1 天**（往前追溯至最近一筆自訂 row 逐期級聯），結束 = 起始 + 1 個月 − 1 天
3. 完全沒有任何自訂 row → 自然月（1 日～最後一日），**全系統行為與導入前完全相同**（月標籤維持 `yyyy-MM`）

期界固定 00:00（UI 只選日期）。唯一硬性驗證：**結束 ≥ 起始**。

### 12.3 空窗 / 重疊語意（重要）

期別間允許空窗或重疊 — 這是使用者自己的選擇，設定頁**即時警告但不阻擋儲存**：

- **空窗段的用電不會被任何期計入**
- **重疊段會被兩期重複計入**

### 12.4 生效範圍

- 用電報表、冷凍噸報表月粒度：YYYY-MM 選擇器語意 = 期別（後端轉換，前端仍傳 `YYYY-MM-01`）；月粒度選擇器旁顯示期別實際區間提示
- 用電報表日粒度：預設起訖 = 本期（今天所屬期別）起始～結束（僅預設值，可自由改）
- EMS 頁：月圖（年檢視 12 期）與日圖（選 YYYY-MM 展開該期每日）皆以期別範圍展開；日圖加總 = 月圖該期柱值
- **年粒度維持自然年**（1/1～12/31）不隨期別偏移
- **能源申報固定曆月**（`GetCalendarMonthlyReportAsync`），不受期別設定影響
- 月報 on-demand、無月度預聚合 → 設定改動**即時生效且不需回填**；修改過去期別後歷史月報用新設定重算，系統不保存期別變更史

### 12.5 設定頁操作

選年份（可前後切換）→ 12 期列表逐期編輯。某期結束日期存檔後，其後未自訂期別的起始自動級聯帶入（結束 +1 天）；
編輯中即時預覽級聯與空窗/重疊警告。「還原預設」刪除該期自訂 row，回到推導值。
唯讀 API `api/current`（今天所屬期別）與 `api/range`（期別區間）供報表頁取用。
