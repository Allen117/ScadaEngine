# 功能說明書：迴路資訊（電力 / 水 / 氣）

## 1. 功能概述

EMS navbar「迴路資訊」dropdown 底下的三個頁面，版面與操作完全一致，差別只在能源別：

| 頁面 | 路由 | 迴路來源 | 單位 | 圖表色 |
|------|------|----------|------|--------|
| 電力 | `/ElectricityCircuitInfo` | `EnergyCircuit` | kWh | 綠 `#2e7d32` |
| 水 | `/WaterCircuitInfo` | `WaterMeterCircuit` | m³ | 藍 `#0277bd` |
| 氣 | `/GasCircuitInfo` | `GasMeterCircuit` | m³ | 橘 `#e65100` |

版面：**左欄迴路階層樹 + 右欄三張長條圖**（年／各月、月／各日、日／各時）。
點左樹任一節點 → 右邊三張圖同時換成該迴路的資料；日卡片每 60 秒自動刷新。
頁面高度由 JS `fitPageHeight()` 依視窗算，整頁不出現捲軸。

> 2026-08-05 由原單一頁 `/CircuitInfo`（僅電力）拆成三頁；舊路由已移除，未保留 redirect。

## 2. 路由 & 權限

| 方法 | 路由 | 說明 | 認證 |
|------|------|------|------|
| GET | `/ElectricityCircuitInfo` | 電力迴路資訊 | 需登入 |
| GET | `/WaterCircuitInfo` | 水迴路資訊 | 需登入 |
| GET | `/GasCircuitInfo` | 氣迴路資訊 | 需登入 |

- 三頁各自登錄於 `PermissionService.ConfigurablePages`，可在 `/AccountSetting` **逐頁**勾選
  （廠務只給水、能源組只給電之類的分授權）。
- 三頁皆在 `PermissionService.EmsRoutes` 內 → 自動套 EMS 淡綠主題。
- Controller 內 `CanAccessPage()` 不過 → `Redirect("/EMS")`（直連也擋得住）。
- navbar dropdown 三項各自 `canAccess()` 過濾；三項全無權限時整個 dropdown 不顯示。

## 3. 資料來源 API（皆為既有 API，本功能未新增後端）

| 頁面 | 迴路樹 | 用量資料 |
|------|--------|----------|
| 電力 | `GET /EMS/api/circuit-tree` | `GET /EMS/api/circuit-energy` |
| 水 | `GET /EMS/api/water-circuit-tree` | `GET /EMS/api/water-usage` |
| 氣 | `GET /EMS/api/gas-circuit-tree` | `GET /EMS/api/gas-usage` |

三組用量 API 的查詢參數與回傳契約完全一致：

```
?circuitId={id}&granularity={month|day|hour}&pivot={2026 | 2026-08 | 2026-08-05}
→ { labels: string[], values: number[], hasWarning?: bool }
```

- `granularity` 語意：`month` = 一年看各月、`day` = 一個月看各日、`hour` = 一天看各時。
- **月檢視的起訖日各自吃自己的月結期別**（電費 `BillingPeriods` / 水費 `WaterBillingPeriods` /
  氣費 `GasBillingPeriods`，氣費含 `IsSkipped` 兩月一期），由後端 `ParsePivotAsync` /
  `ParseWaterPivotAsync` / `ParseGasPivotAsync` 吸收，前端不需要知道差異。
- `hasWarning`：**水/氣才有**，代表區間內有時段的表資料不完整（表斷線）。電力 API 無此欄位。

## 4. 前端架構

三頁共用 `wwwroot/js/ems-circuit.js` 一支（IIFE），差異以 **`data-kind`** 表達：

```html
<div class="ems-circuit-page" data-kind="water">   <!-- electricity | water | gas -->
```

JS 內 `CONFIG` 表持有三組 `{ treeUrl, usageUrl, unit, fill, border }`，
`init()` 讀 `data-kind` 決定 `_cfg`，其餘邏輯（建樹、選節點、三張圖 fetch/render、60s 輪詢）完全共用。

**新增第四種能源時**：在 `CONFIG` 加一組 + 複製一份 View 改 `data-kind` 與標題文字即可，JS 邏輯不必動。

資料缺漏警示（水/氣）：View 在每張卡片 body 內放

```html
<div class="ems-circuit-warn small text-warning" data-warn="hour" style="display:none;">部分時段水表資料不完整（可能斷線）</div>
```

JS `setWarn(granularity, isWarn)` 依該次 API 的 `hasWarning` 切換顯隱；電力頁沒有這些元素 → 自動 no-op。

## 5. 檔案位置

| 檔案 | 用途 |
|------|------|
| `Features/ElectricityCircuitInfo/Controllers/ElectricityCircuitInfoController.cs` | 電力頁 Controller（僅權限判斷 + 回 View）|
| `Features/ElectricityCircuitInfo/Views/Index.cshtml` | 電力頁版面 |
| `Features/WaterCircuitInfo/…`、`Features/GasCircuitInfo/…` | 同上，水 / 氣 |
| `wwwroot/js/ems-circuit.js` | 三頁共用邏輯（CONFIG 表 + 樹 + 圖表 + 輪詢）|
| `wwwroot/css/ems.css` | `.ems-circuit-*` / `.ems-chart-*` / `.ems-tree-*` 版面樣式 |
| `Views/Shared/_Layout.cshtml` | navbar「迴路資訊」dropdown |
| `Resources/Views.Shared._Layout.{,en}.resx` | 選單字串（`layout.menu.{electricity,water,gas}_circuit_info`）|

## 6. i18n

頁面本身**不在 i18n 範圍**（字串為 zh-TW 硬寫，同原 `/CircuitInfo`）；
只有 navbar 選單字走 `Views.Shared._Layout.{,en}.resx`：

| key | zh-TW | en |
|-----|-------|----|
| `layout.menu.circuit_info` | 迴路資訊 | Circuit Info |
| `layout.menu.electricity_circuit_info` | 電力 | Electricity |
| `layout.menu.water_circuit_info` | 水 | Water |
| `layout.menu.gas_circuit_info` | 氣 | Gas |

## 7. 已知限制

- 迴路樹預設選「第一個根節點」。水/氣體系設計上為單一根迴路（全廠），因此等同預設選全廠。
- 未建任何迴路時，樹為空、三張圖維持空白（不報錯）。
- 電力頁的已知加總語意限制（主錶綁 SID 又掛子迴路時會連自身一起加總）與 `/EnergyReport` 相同，
  詳見 [功能說明書_EMS頁面.md](功能說明書_EMS頁面.md) §7.5。
