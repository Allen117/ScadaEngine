bin/
obj/
*.user
*.suo
.vs/
*.log
*.tmp
*.map

# Build output (case-insensitive: publish/ and Publish/)
publish/
Publish/
**/publish/
**/Publish/

# Python virtual environment
ScadaEngine.Engine/PythonRuntime/
__pycache__/
*.pyc

# Node modules
node_modules/

# Release output
Release/

# 現場工程工具包（大型安裝檔 + 測試工具）不進 git，改由內部雲端下載，見 Tools/README.md
Tools/

# Claude Code (personal memory only)
.claude/projects/

# Claude Code skill lock 只放在 repo root，不該出現在子專案
ScadaEngine.Web/skills-lock.json
ScadaEngine.Engine/skills-lock.json

# Sensitive config (uncomment if repo is public)
# ScadaEngine.Engine/Setting/dbSetting.json

# Line Messaging API token (must NEVER be committed)
ScadaEngine.Engine/Setting/LineSetting.json

# OPC UA 現場點位設定（僅 *.json.example 進版控，實際設定各機自管）
ScadaEngine.Engine/OpcUaPoint/*.json

# HTTPS 憑證（CA 私鑰 / 伺服器憑證 / CA 公開憑證）各機自行以 certs/generate-https-cert.ps1 產生，不進 git
# 僅保留 certs/*.ps1 腳本
ScadaEngine.Web/certs/*.pfx
ScadaEngine.Web/certs/*.crt
ScadaEngine.Web/certs/*.cer